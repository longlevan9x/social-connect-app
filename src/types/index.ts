export interface FacebookRequest extends Request {
    user?: any; // Add user type if you have a specific user model
}

export interface FacebookResponse extends Response {
    // Add any custom response properties if needed
}